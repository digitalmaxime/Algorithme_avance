Algorithme avancé
cours d'algorithme avancé de poly

setup run -> python3 convert_to_instances.py # will generate adjacences lists

1)    
    run -> main.sh
    
    # which will run for each instances and algo:
    # run -> ./tp.sh -a 'algo' -e 'path_to_matrix' -p -t

2) For one instance : 
    run ./tp.sh -a [glouton | branch_bound | tabou] -e [path_vers_exemplaire] [-p] [-t]

